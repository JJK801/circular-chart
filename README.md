# [POC] Dynamic circular chart with url data

## Usage

```shell
yarn
yarn start
```

Go to http://localhost:5000?data[producteur]=5.6&data[distributeur]=2.05&data[tva]=1.03

Then you can change/add/remove `data` items in URL.
